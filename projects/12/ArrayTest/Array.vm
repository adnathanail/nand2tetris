function Array.new 0
  // return
  push argument 0
  call Memory.alloc 1
  return
function Array.dispose 0
  push argument 0
  pop pointer 0
  // do
  push pointer 0
  call Memory.deAlloc 1
  pop temp 0
  // return
  push constant 0
  return
