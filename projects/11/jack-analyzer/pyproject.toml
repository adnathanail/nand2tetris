[project]
name = "jack-analyzer"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.1.0",
    "ruff>=0.11.8",
]

[tool.ruff.lint]
extend-select = ["B"]

[tool.black]
line-length = 150
